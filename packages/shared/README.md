# Shared Packages

Shared dependencies and utilities used across all JCW services.

## Planned Modules
- `rates` — Rate lookup and override logic (used by payroll + billing)
- `time` — Date/time utilities, week boundaries, holiday detection
- `db` — Database connection helpers
- `auth` — Shared authentication middleware